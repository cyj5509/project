<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devday.mapper.UserMapper">

<!-- 회원가입: 해당 컬럼 제외한 나머지 컬럼들은 Default로 설정한 값들로 저장 -->
<insert id="join" parameterType="com.devday.domain.UserVO">
	INSERT INTO 
		user_table (
			us_id, 
			us_pw, 
			us_name, 
			us_phone, 
			us_email, 
			us_postcode, 
			us_addr_basic, 
			us_addr_detail
		)
		VALUES (
			#{us_id}, 
			#{us_pw}, 
			#{us_name}, 
			#{us_phone}, 
			#{us_email}, 
			#{us_postcode}, 
			#{us_addr_basic}, 
			#{us_addr_detail}
		)
</insert>

<!-- 아이디 중복 검사 -->
<!-- resultType="int" 또는 "Integer"일 경우, 'SELECT COUNT(*) ~'의 형태로 작성 -->
<select id="idCheck" parameterType="String" resultType="String">
	SELECT
		us_id
	FROM
		user_table
	WHERE
		us_id = #{us_id}
</select>

<!-- 로그인: 사용자/관리자를 구분하기 위해 select문에 'ad_check' 컬럼 추가 -->
<select id="login" parameterType="String" resultType="com.devday.domain.UserVO">
	select 
		us_id, 
		us_pw, 
		us_name, 
		us_phone, 
		us_email, 
		us_postcode, 
		us_addr_basic, 
		us_addr_detail, 
		us_point, 
		us_join_date, 
		us_update_date,
		us_last_login,
		us_status,
		ad_check
	from 
		user_table 
	where 
		us_id = #{us_id}
</select>

<!-- 최근 접속 일자: 로그인 관련 메서드 내에서 사용 -->
<!-- sysdate로 갱신 필요(us_lastlogin DATE DEFAULT sysdate NOT NULL) -->
<update id="lastLoginTime" parameterType="String">
	UPDATE
		user_table
	SET
		us_last_login = sysdate
	WHERE
		us_id = #{us_id}
</update>

<!-- 아이디 찾기: 이름과 이메일을 이용한 찾기 -->
<select id="findId" parameterType="com.devday.dto.FindInfoDTO" resultType="com.devday.domain.UserVO">
	SELECT
		us_id, us_join_date
	FROM
		user_table
	WHERE
		us_name = #{us_name} AND us_email = #{us_email}
</select>

<!-- 비밀번호 찾기: 아이디 및 이름과 이메일을 이용한 찾기 -->
<select id="findPw" parameterType="com.devday.dto.FindInfoDTO" resultType="int">
	 SELECT
	 	COUNT(*)
	 FROM
	 	user_table
	 WHERE
	 	us_id = #{us_id} AND us_name = #{us_name} AND us_email = #{us_email}
</select>

<!-- 비밀번호 업데이트: 임시 비밀번호 생성 및 비밀번호 재발급 -->
<update id="resetPw" parameterType="com.devday.dto.FindInfoDTO">
	UPDATE 
		user_table
	SET
		us_pw = #{us_pw}
	WHERE
		us_id = #{us_id}
</update>

<select id="isPwMatch" resultType="String" parameterType="String">
	SELECT 
		us_pw 
	FROM 
		user_table
	WHERE 
		us_id = #{us_id}
</select>

<!-- 회원수정: sysdate로 갱신 필요(us_updatedate DATE DEFAULT sysdate NOT NULL) -->
<update id="modify" parameterType="com.devday.domain.UserVO">
	UPDATE
		user_table
	SET
		us_phone = #{us_phone},
		us_email = #{us_email}, 
		us_postcode = #{us_postcode}, 
		us_addr_basic = #{us_addr_basic}, 
		us_addr_detail = #{us_addr_detail},
		us_update_date = sysdate
	WHERE 
		us_id = #{us_id}
</update>

<!-- 회원탈퇴 -->
<delete id="delete" parameterType="String">
	DELETE FROM
		user_table
	WHERE 
		us_id = #{us_id}
</delete>

</mapper>  