<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devday.mapper.MemberMapper">

<!-- 회원가입: 나머지 컬럼들은 Default로 설정한 값들이 있어 따로 기입하지 않아도 됨 -->
<insert id="join" parameterType="com.devday.domain.MemberVO">
	INSERT INTO 
		member_tbl (
			mem_id, 
			mem_pw, 
			mem_name, 
			mem_phone, 
			mem_email, 
			mem_postcode, 
			mem_addr, 
			mem_deaddr
		)
		VALUES (
			#{mem_id}, 
			#{mem_pw}, 
			#{mem_name}, 
			#{mem_phone}, 
			#{mem_email}, 
			#{mem_postcode}, 
			#{mem_addr}, 
			#{mem_deaddr}
		)
</insert>

<!-- 아이디 중복검사: String mem_id -> parameterType="String" -->
<!-- resultType="int" 또는 "Integer"일 경우, SELECT COUNT(*) FROM member_tbl WHERE mem_id = #{mem_id} -->
<select id="idCheck" parameterType="String" resultType="String">
	SELECT
		mem_id
	FROM
		member_tbl
	WHERE
		mem_id = #{mem_id}
</select>

<!-- 로그인: 관리자/사용자를 구분 짓기 위해 select문에 adm_check(회원 상태) 컬럼 추가 -->
<select id="login" parameterType="String" resultType="com.devday.domain.MemberVO">
	select 
		mem_id, 
		mem_pw, 
		mem_name, 
		mem_phone, 
		mem_email, 
		mem_postcode, 
		mem_addr, 
		mem_deaddr, 
		mem_point, 
		mem_joindate, 
		mem_updatedate,
		mem_lastlogin,
		adm_check
	from 
		member_tbl 
	where 
		mem_id = #{mem_id}
</select>

<!-- 접속일자 업데이트: 로그인 관련 메서드 내에서 사용 -->
<!-- sysdate로 갱신 필요(mem_lastlogin DATE DEFAULT sysdate NOT NULL) -->
<update id="loginTimeUpdate" parameterType="String">
	UPDATE
		member_tbl
	SET
		mem_lastlogin = sysdate
	WHERE
		mem_id = #{mem_id}
</update>

<!-- 아이디 찾기: 이름과 이메일을 이용한 찾기 -->
<select id="findId" parameterType="com.devday.dto.FindInfoDTO" resultType="String">
	SELECT
		mem_id
	FROM
		member_tbl
	WHERE
		mem_name = #{mem_name} AND mem_email = #{mem_email}
</select>

<!-- 비밀번호 찾기: 아이디 및 이름과 이메일을 이용한 찾기 -->
<select id="findPw" parameterType="com.devday.dto.FindInfoDTO" resultType="int">
	 SELECT
	 	COUNT(*)
	 FROM
	 	member_tbl
	 WHERE
	 	mem_id = #{mem_id} AND mem_name = #{mem_name} AND mem_email = #{mem_email}
</select>

<!-- 비밀번호 업데이트: 임시 비밀번호 생성 및 비밀번호 재발급 -->
<update id="resetPw" parameterType="com.devday.dto.FindInfoDTO">
	UPDATE 
		member_tbl
	SET
		mem_pw = #{mem_pw}
	WHERE
		mem_id = #{mem_id}
</update>

<!-- 회원수정: sysdate로 갱신 필요(mem_updatedate DATE DEFAULT sysdate NOT NULL) -->
<update id="modify" parameterType="com.devday.domain.MemberVO">
	UPDATE
		member_tbl
	SET
		mem_phone = #{mem_phone},
		mem_email = #{mem_email}, 
		mem_postcode = #{mem_postcode}, 
		mem_addr = #{mem_addr}, 
		mem_deaddr = #{mem_deaddr},
		mem_updatedate = sysdate
	WHERE 
		mem_id = #{mem_id}
</update>

<!-- 회원탈퇴 -->
<delete id="delete" parameterType="String">
	DELETE FROM
		member_tbl
	WHERE 
		mem_id = #{mem_id}
</delete>

</mapper>  