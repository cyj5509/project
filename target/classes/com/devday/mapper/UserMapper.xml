<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devday.mapper.UserMapper">

<!-- 회원가입: 나머지 컬럼들은 Default로 설정한 값들이 있어 따로 기입하지 않아도 됨 -->
<insert id="join" parameterType="com.devday.domain.UserVO">
	INSERT INTO 
		us_tbl (
			us_id, 
			us_pw, 
			us_name, 
			us_phone, 
			us_email, 
			us_postcode, 
			us_addr_basic, 
			us_addr_detail
		)
		VALUES (
			#{us_id}, 
			#{us_pw}, 
			#{us_name}, 
			#{us_phone}, 
			#{us_email}, 
			#{us_postcode}, 
			#{us_addr_basic}, 
			#{us_addr_detail}
		)
</insert>

<!-- 아이디 중복검사: String us_id -> parameterType="String" -->
<!-- resultType="int" 또는 "Integer"일 경우, SELECT COUNT(*) FROM us_tbl WHERE us_id = #{us_id} -->
<select id="idCheck" parameterType="String" resultType="String">
	SELECT
		us_id
	FROM
		us_tbl
	WHERE
		us_id = #{us_id}
</select>

<!-- 로그인: 관리자/사용자를 구분 짓기 위해 select문에 us_status(회원 상태) 컬럼 추가 -->
<select id="login" parameterType="String" resultType="com.devday.domain.UserVO">
	select 
		us_id, 
		us_pw, 
		us_name, 
		us_phone, 
		us_email, 
		us_postcode, 
		us_addr_basic, 
		us_addr_detail, 
		us_point, 
		us_join_date, 
		us_update_date,
		us_last_login,
		us_status
	from 
		us_tbl 
	where 
		us_id = #{us_id}
</select>

<!-- 접속일자 업데이트: 로그인 관련 메서드 내에서 사용 -->
<!-- sysdate로 갱신 필요(us_lastlogin DATE DEFAULT sysdate NOT NULL) -->
<update id="loginTimeUpdate" parameterType="String">
	UPDATE
		us_tbl
	SET
		us_last_login = sysdate
	WHERE
		us_id = #{us_id}
</update>

<!-- 아이디 찾기: 이름과 이메일을 이용한 찾기 -->
<select id="findId" parameterType="com.devday.dto.FindInfoDTO" resultType="com.devday.domain.UserVO">
	SELECT
		us_id, us_join_date
	FROM
		us_tbl
	WHERE
		us_name = #{us_name} AND us_email = #{us_email}
</select>

<!-- 비밀번호 찾기: 아이디 및 이름과 이메일을 이용한 찾기 -->
<select id="findPw" parameterType="com.devday.dto.FindInfoDTO" resultType="int">
	 SELECT
	 	COUNT(*)
	 FROM
	 	us_tbl
	 WHERE
	 	us_id = #{us_id} AND us_name = #{us_name} AND us_email = #{us_email}
</select>

<!-- 비밀번호 업데이트: 임시 비밀번호 생성 및 비밀번호 재발급 -->
<update id="resetPw" parameterType="com.devday.dto.FindInfoDTO">
	UPDATE 
		us_tbl
	SET
		us_pw = #{us_pw}
	WHERE
		us_id = #{us_id}
</update>

<select id="isPwMatch" resultType="String" parameterType="String">
	SELECT 
		us_pw 
	FROM 
		us_tbl
	WHERE 
		us_id = #{us_id}
</select>

<!-- 회원수정: sysdate로 갱신 필요(us_updatedate DATE DEFAULT sysdate NOT NULL) -->
<update id="modify" parameterType="com.devday.domain.UserVO">
	UPDATE
		us_tbl
	SET
		us_phone = #{us_phone},
		us_email = #{us_email}, 
		us_postcode = #{us_postcode}, 
		us_addr_basic = #{us_addr_basic}, 
		us_addr_detail = #{us_addr_detail},
		us_update_date = sysdate
	WHERE 
		us_id = #{us_id}
</update>

<!-- 회원탈퇴 -->
<delete id="delete" parameterType="String">
	DELETE FROM
		us_tbl
	WHERE 
		us_id = #{us_id}
</delete>

</mapper>  